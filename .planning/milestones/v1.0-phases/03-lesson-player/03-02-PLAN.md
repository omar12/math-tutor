---
phase: 03-lesson-player
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/components/StepCard.tsx
  - src/components/ConfettiScreen.tsx
autonomous: true
requirements:
  - LESS-01
  - LESS-03
must_haves:
  truths:
    - "@keyframes confetti-fall and .confetti-piece rule exist in src/index.css"
    - "src/components/StepCard.tsx renders step text in p.text-xl, optional equation in p.text-4xl.font-bold centered, and replay button with aria-label='Replay this step'"
    - "StepCard replay button is min-h-[44px] min-w-[44px] with style touchAction: manipulation"
    - "src/components/ConfettiScreen.tsx renders exactly 40 confetti div.confetti-piece elements with aria-hidden='true' on the container"
    - "ConfettiScreen renders a 'You did it!' h1 and a 'Start Practice' button with min-h-[64px] min-w-[200px] bg-accent (#3B82F6)"
    - "ConfettiScreen 'Start Practice' button has aria-label='Start Practice' and calls onStartPractice prop on click"
    - "./node_modules/.bin/tsc --noEmit exits 0 after adding both components"
  artifacts:
    - path: src/components/StepCard.tsx
      provides: "Renders one LessonStep: instruction text (text-xl), equation (text-4xl bold, optional), replay button (44px min touch target)"
    - path: src/components/ConfettiScreen.tsx
      provides: "Celebration screen: 40 confetti pieces (CSS confetti-fall), 'You did it!' h1, 'Start Practice' button"
  key_links:
    - from: src/screens/LessonScreen.tsx
      to: src/components/StepCard.tsx
      via: "import StepCard"
      pattern: "import StepCard from '\\.\\./components/StepCard'"
    - from: src/screens/LessonScreen.tsx
      to: src/components/ConfettiScreen.tsx
      via: "import ConfettiScreen"
      pattern: "import ConfettiScreen from '\\.\\./components/ConfettiScreen'"
---

<objective>
Create the two new UI components for Phase 3 — StepCard and ConfettiScreen — and add the confetti CSS keyframe animation to src/index.css.

These components are pure presentational: StepCard renders one LessonStep (text + optional equation + replay button), ConfettiScreen renders the lesson-end celebration. Both components accept callbacks as props; audio playback and navigation are handled by LessonScreen (Plan 03).

This plan can execute in parallel with Plan 03-01 because neither component imports howler.

Output: src/components/StepCard.tsx, src/components/ConfettiScreen.tsx, updated src/index.css.
</objective>

<execution_context>
@/Users/omar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/omar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/omar/projects/math-tutor/.planning/PROJECT.md
@/Users/omar/projects/math-tutor/.planning/ROADMAP.md
@/Users/omar/projects/math-tutor/.planning/phases/03-lesson-player/03-CONTEXT.md
@/Users/omar/projects/math-tutor/.planning/phases/03-lesson-player/03-RESEARCH.md
@/Users/omar/projects/math-tutor/.planning/phases/03-lesson-player/03-UI-SPEC.md

<interfaces>
From src/curriculum/types.ts (existing — StepCard consumes LessonStep):

```typescript
export interface LessonStep {
  text: string              // narration display text for this step
  narrationAudio: string    // e.g. '/audio/lessons/addition-grade1-01/step-1.mp3'
  equation?: string         // optional Unicode expression e.g. '3 + 4 = ?'
}
```

From src/screens/HomeScreen.tsx (existing — copy full-screen layout pattern):

```typescript
// Full-screen container pattern:
<div
  className="flex flex-col items-center justify-center h-full bg-surface gap-8 px-6"
  style={{ height: '100dvh', paddingBottom: 'env(safe-area-inset-bottom)' }}
>

// Touch button pattern (PLAT-01 / CLAUDE.md):
<button
  className="bg-primary text-white text-2xl font-bold rounded-3xl px-10 py-5 min-h-[64px] min-w-[200px] active:opacity-80"
  style={{ touchAction: 'manipulation' }}
>
```

From src/index.css (existing @theme tokens to use):
```
--color-primary:    #FF6B35   ← replay icon active state, Next indicator
--color-secondary:  #FFD23F   ← confetti yellow
--color-accent:     #3B82F6   ← "Start Practice" button bg
--color-success:    #22C55E   ← confetti green
--color-surface:    #FFF8F0   ← all screen backgrounds
--color-on-surface: #1C1917   ← text color
```

UI-SPEC confetti palette (5 colors, 40 pieces):
- '#FF6B35', '#FFD23F', '#3B82F6', '#22C55E', '#FF4757'

UI-SPEC StepCard layout:
- White card: `bg-white rounded-2xl shadow-sm p-8`
- Width: `w-4/5 max-w-lg`
- Replay icon: circular arrow SVG, #FF6B35, 44×44px
- Equation block: `text-4xl font-bold text-center text-on-surface`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add confetti CSS keyframes to src/index.css</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css — read current file end to append without duplicating existing rules
    - .planning/phases/03-lesson-player/03-UI-SPEC.md — CSS Contract section (exact @keyframes and .confetti-piece rule)
    - .planning/phases/03-lesson-player/03-RESEARCH.md — Pattern 4 (CSS confetti animation)
  </read_first>
  <action>
    Append to the end of src/index.css (after the existing button/[role="button"]/a rule block):

    ```css
    /* Phase 3: Lesson Player — Confetti celebration animation */
    @keyframes confetti-fall {
      0%   { transform: translateY(-10px) rotate(0deg);   opacity: 1; }
      100% { transform: translateY(110vh) rotate(720deg); opacity: 0; }
    }

    .confetti-piece {
      animation: confetti-fall 1.8s ease-in forwards;
      border-radius: 2px;
      position: fixed;
    }
    ```

    Do not modify any existing rule. Do not remove any existing content.
  </action>
  <verify>
    <automated>cd /Users/omar/projects/math-tutor && grep -c "confetti-fall" src/index.css</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css contains `@keyframes confetti-fall` (grep confirms)
    - src/index.css contains `.confetti-piece` class rule (grep confirms)
    - The animation duration is `1.8s` (grep: `1.8s ease-in forwards`)
    - Existing rules above the addition are unchanged (no whitespace or ordering changes to the @theme block)
  </acceptance_criteria>
  <done>confetti-fall keyframe and .confetti-piece rule appended to src/index.css.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/components/StepCard.tsx</name>
  <files>src/components/StepCard.tsx</files>
  <read_first>
    - src/components/RemyFox.tsx — existing component style: functional component, TypeScript props interface, named export
    - src/screens/HomeScreen.tsx — button pattern for touch targets (touchAction: manipulation, min-h-[44px])
    - .planning/phases/03-lesson-player/03-UI-SPEC.md — Component Inventory (StepCard anatomy) and Step Screen ASCII layout
    - .planning/phases/03-lesson-player/03-CONTEXT.md — D-05 (replay icon per step), D-07 (full-screen per-step, no scroll)
  </read_first>
  <action>
    Create src/components/StepCard.tsx. Props interface:

    ```typescript
    interface StepCardProps {
      text: string
      equation?: string
      onReplay: () => void
    }
    ```

    Render structure (following UI-SPEC Step Screen anatomy):
    - Outer container: white card `bg-white rounded-2xl shadow-sm p-8 w-4/5 max-w-lg` — no overflow, no scroll
    - Step instruction text: `<p className="text-xl text-on-surface leading-relaxed">{text}</p>`
    - Equation block (only when equation is truthy):
      `<p className="text-4xl font-bold text-center text-on-surface mt-8">{equation}</p>`
    - Replay button (always present, bottom-right of card):
      - `<button>` element with `aria-label="Replay this step"`
      - `className="self-end mt-6 min-h-[44px] min-w-[44px] flex items-center justify-center"`
      - `style={{ touchAction: 'manipulation' }}`
      - `onClick={onReplay}`
      - Icon: inline SVG circular arrow (clockwise, 24×24, stroke #FF6B35, fill none, strokeWidth 2):
        ```
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#FF6B35" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">
          <path d="M3 12a9 9 0 1 0 9-9 9.75 9.75 0 0 0-6.74 2.74L3 8"/>
          <path d="M3 3v5h5"/>
        </svg>
        ```

    Card layout direction: `flex flex-col`. No imports except React and the LessonStep type. Use named export: `export function StepCard(...)`.

    Do not import howler — StepCard is a pure presentation component.
  </action>
  <verify>
    <automated>cd /Users/omar/projects/math-tutor && ./node_modules/.bin/tsc --noEmit 2>&1 | grep "StepCard" || echo "StepCard.tsx: no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/StepCard.tsx exists
    - File exports `StepCard` as a named export (grep: `export function StepCard`)
    - `aria-label="Replay this step"` is present on the button (grep confirms)
    - Equation `<p>` contains `text-4xl font-bold` classes (grep confirms)
    - `touchAction: 'manipulation'` is present on the replay button (grep: `touchAction`)
    - `./node_modules/.bin/tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>StepCard.tsx exists with named export, replay button (44px, aria-label), equation block (text-4xl bold, optional). tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Create src/components/ConfettiScreen.tsx</name>
  <files>src/components/ConfettiScreen.tsx</files>
  <read_first>
    - src/screens/HomeScreen.tsx — 100dvh layout pattern, button touch target pattern with min-h-[64px]
    - .planning/phases/03-lesson-player/03-UI-SPEC.md — Celebration Screen anatomy, Copywriting Contract ("You did it!", "Start Practice"), Color (confetti 5-color palette, bg-accent #3B82F6)
    - .planning/phases/03-lesson-player/03-RESEARCH.md — Pattern 4 (confetti generation: deterministic left values, 5 colors, 40 pieces)
    - .planning/phases/03-lesson-player/03-CONTEXT.md — D-08 (no Remy on celebration screen), D-09 (no auto-advance)
  </read_first>
  <action>
    Create src/components/ConfettiScreen.tsx. Props interface:

    ```typescript
    interface ConfettiScreenProps {
      onStartPractice: () => void
    }
    ```

    Component constants (define before the component function):
    ```typescript
    const CONFETTI_COLORS = ['#FF6B35', '#FFD23F', '#3B82F6', '#22C55E', '#FF4757'] as const
    const PIECE_COUNT = 40
    ```

    Generate confetti pieces deterministically (no Math.random() — avoids hydration issues and test instability):
    ```typescript
    const pieces = Array.from({ length: PIECE_COUNT }, (_, i) => ({
      id: i,
      color: CONFETTI_COLORS[i % CONFETTI_COLORS.length],
      left: `${(i * 2.5 + Math.floor(i / 5) * 3) % 100}%`,
      delay: `${(i * 0.07) % 1.5}s`,
      size: i % 3 === 0 ? 10 : 8,
    }))
    ```

    Render structure (following UI-SPEC Celebration Screen anatomy):
    - Outer container: `style={{ height: '100dvh', paddingBottom: 'env(safe-area-inset-bottom)', position: 'relative', overflow: 'hidden' }}`
      plus `className="flex flex-col items-center justify-center gap-8 bg-surface"`
    - Confetti container (aria-hidden="true"):
      ```
      <div aria-hidden="true">
        {pieces.map(p => (
          <div
            key={p.id}
            className="confetti-piece"
            style={{ left: p.left, width: p.size, height: p.size, backgroundColor: p.color, animationDelay: p.delay }}
          />
        ))}
      </div>
      ```
    - Heading: `<h1 className="text-3xl font-bold text-on-surface relative">You did it!</h1>`
      (relative so it layers above confetti)
    - "Start Practice" button:
      - `className="bg-accent text-white text-xl font-bold rounded-3xl px-10 py-5 min-h-[64px] min-w-[200px] active:opacity-80 relative"`
      - `style={{ touchAction: 'manipulation' }}`
      - `aria-label="Start Practice"`
      - `onClick={onStartPractice}`
      - Text: `Start Practice`

    Use named export: `export function ConfettiScreen(...)`. No Remy component (D-08). No auto-advance logic.
  </action>
  <verify>
    <automated>cd /Users/omar/projects/math-tutor && ./node_modules/.bin/tsc --noEmit 2>&1 | grep "ConfettiScreen" || echo "ConfettiScreen.tsx: no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ConfettiScreen.tsx exists
    - File exports `ConfettiScreen` as a named export (grep: `export function ConfettiScreen`)
    - `aria-hidden="true"` is on the confetti container (grep confirms)
    - `"You did it!"` text is present (grep confirms)
    - `"Start Practice"` appears as button text and aria-label (grep confirms)
    - `PIECE_COUNT = 40` constant is defined (grep confirms)
    - Button has `min-h-\[64px\]` (grep: `min-h-[64px]`)
    - `./node_modules/.bin/tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ConfettiScreen.tsx exists: 40 confetti pieces, aria-hidden confetti container, 'You did it!' h1, 'Start Practice' button (64px, bg-accent). tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan creates two presentational React components and adds CSS. No user input, network requests, auth, or data persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | src/components/StepCard.tsx | accept | Static React component; onReplay prop is a local callback, not user-controlled data |
| T-03-02-02 | Tampering | src/components/ConfettiScreen.tsx | accept | Static React component; onStartPractice prop triggers navigation, not data mutation |

No meaningful attack surface. ASVS L1 V2–V6 not applicable.
</threat_model>

<verification>
After all three tasks complete:

1. `grep -c "confetti-fall" /Users/omar/projects/math-tutor/src/index.css` — returns 2 (in @keyframes name and .confetti-piece animation property)
2. `grep "export function StepCard" src/components/StepCard.tsx` — matches
3. `grep "aria-label=\"Replay this step\"" src/components/StepCard.tsx` — matches
4. `grep "export function ConfettiScreen" src/components/ConfettiScreen.tsx` — matches
5. `grep "PIECE_COUNT = 40" src/components/ConfettiScreen.tsx` — matches
6. `./node_modules/.bin/tsc --noEmit` — exits 0
</verification>

<success_criteria>
- confetti-fall keyframe and .confetti-piece class added to src/index.css
- StepCard renders text, optional equation (text-4xl bold), replay button (44px, aria-label)
- ConfettiScreen renders 40 confetti pieces (aria-hidden), "You did it!" h1, "Start Practice" button (64px, bg-accent #3B82F6)
- tsc --noEmit exits 0 with no new type errors
</success_criteria>

<output>
After completion, create `/Users/omar/projects/math-tutor/.planning/phases/03-lesson-player/03-02-SUMMARY.md`
</output>
