---
phase: 04-practice-engine
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/components/FeedbackSlot.tsx
  - src/components/ConfettiScreen.tsx
  - src/screens/PracticeScreen.tsx
autonomous: true
requirements: [FEED-01, FEED-02, FEED-03, FEED-04]

must_haves:
  truths:
    - "After a first wrong answer, a warm encouragement phrase appears below the problem question in orange (text-primary)"
    - "After a second wrong answer, the hint text replaces the encouragement in the same slot — displayed in blue (text-accent) to signal a different kind of message — with no layout shift and no overlapping text"
    - "After a third wrong answer, the correct answer is highlighted and the app auto-advances after 1.5 seconds"
    - "After the last problem is answered, the ConfettiScreen appears with a 'Back to Home' button"
    - "Tapping 'Back to Home' on the celebration screen navigates to '/'"
    - "The LessonScreen ConfettiScreen still shows 'Start Practice' (backwards compatibility)"
    - "FeedbackSlot never shows both encouragement text and hint text simultaneously — only one message is visible at any time"
  artifacts:
    - path: "src/components/FeedbackSlot.tsx"
      provides: "Inline encouragement/hint slot with reserved height; visually distinct styles for encouragement vs hint; no layout shift"
      exports: ["FeedbackSlot"]
    - path: "src/components/ConfettiScreen.tsx"
      provides: "buttonLabel?: string prop added; default 'Start Practice'"
      contains: "buttonLabel"
    - path: "src/screens/PracticeScreen.tsx"
      provides: "FeedbackSlot wired; ConfettiScreen with buttonLabel='Back to Home'; aria-live assertive region for reveal announcement"
      contains: "Back to Home"
  key_links:
    - from: "src/screens/PracticeScreen.tsx"
      to: "src/components/FeedbackSlot.tsx"
      via: "FeedbackSlot attemptCount={state.attemptCount} phraseIndex={state.phraseIndex}"
      pattern: "FeedbackSlot"
    - from: "src/screens/PracticeScreen.tsx"
      to: "src/components/ConfettiScreen.tsx"
      via: "buttonLabel='Back to Home'"
      pattern: "Back to Home"
---

<objective>
Wire feedback escalation and session end. After this plan the full practice loop is complete: wrong answers show warm encouragement, a second wrong answer shows the hint (visually distinct from encouragement), a third wrong answer reveals the correct answer and auto-advances, and the session ends with the ConfettiScreen.

Purpose: Closes FEED-01 through FEED-04. The plan delivers one more vertical slice — the child can now fail problems, see helpful responses, and reach the celebration.

Output: FeedbackSlot component (with visual distinction between encouragement and hint), ConfettiScreen prop extension, PracticeScreen wired with both.

Review notes applied (from 04-REVIEWS.md):
- HIGH: Feedback escalation transition clarity — encouragement (text-primary / orange) and hint (text-accent / blue) are visually distinct; never overlap; hint replaces encouragement in the same reserved slot
- HIGH: Input locking during reveal — aria-live="assertive" region announces correct answer before auto-advance; state.phase guard already in PracticeScreen callbacks (Plan 01) prevents additional dispatches
- MEDIUM: Visual feedback clarity — FeedbackSlot uses different color and optional icon-adjacent styling for hint vs encouragement (color alone is sufficient per UI-SPEC; both remain text-only for visual calm)
- MEDIUM: aria-live="assertive" upgrade for the correct-reveal announcement (separate from the FeedbackSlot polite region)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-practice-engine/04-CONTEXT.md
@.planning/phases/04-practice-engine/04-UI-SPEC.md
@.planning/phases/04-practice-engine/04-PATTERNS.md
@.planning/phases/04-practice-engine/04-01-SUMMARY.md
</context>

<interfaces>
From src/components/ConfettiScreen.tsx (current — read before modifying):
  interface ConfettiScreenProps { onStartPractice: () => void }
  Button: aria-label="Start Practice" onClick={onStartPractice} — both text and aria-label must use the prop after modification

From src/screens/PracticeScreen.tsx (after Plan 01 — read before modifying):
  PracticeState.attemptCount: number
  PracticeState.phraseIndex: number
  PracticeState.phase: 'answering' | 'revealing' | 'celebration'
  FeedbackSlot placeholder: inline div className="min-h-[48px]" aria-hidden="true" — replace with FeedbackSlot component
  Callbacks: handleCorrectAnswer, handleWrongAnswer both guard `if (state.phase !== 'answering') return` — do not remove these guards

FeedbackSlot logic (from PATTERNS.md):
  attemptCount === 0 → null (invisible, height reserved)
  attemptCount === 1 → ENCOURAGEMENT_PHRASES[phraseIndex % phrases.length] — styled text-primary (orange, warm)
  attemptCount >= 2 → FALLBACK_HINT = "Think about what number makes the equation balance." — styled text-accent (blue, guidance)
  // Visual distinction rule: encouragement = orange (text-primary); hint = blue (text-accent). Never both visible at once.
  // No hint field in curriculum schema (RESEARCH.md verified) — always use fallback

ENCOURAGEMENT_PHRASES (5 phrases, from UI-SPEC):
  "Nice try! Give it another go."
  "Almost! You can do it."
  "Keep going — you're doing great!"
  "Not quite, but don't give up!"
  "Hmm, try again!"

Correct-reveal ARIA announcement (from UI-SPEC Copywriting Contract):
  "The answer is {N}. Moving to the next problem." — aria-live="assertive" region in PracticeScreen (NOT in FeedbackSlot)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FeedbackSlot component and ConfettiScreen prop extension</name>
  <files>src/components/FeedbackSlot.tsx, src/components/ConfettiScreen.tsx</files>
  <read_first>
    - src/components/ConfettiScreen.tsx — full file; current props interface; button aria-label and text to update; LessonScreen test at src/screens/LessonScreen.test.tsx line ~58 asserts getByRole('button', { name: /start practice/i }) — default must preserve this
    - src/screens/LessonScreen.test.tsx — find the test that asserts the ConfettiScreen button label to verify regression expectation
    - .planning/phases/04-practice-engine/04-PATTERNS.md — FeedbackSlot implementation excerpt (FeedbackSlotProps interface; ENCOURAGEMENT_PHRASES array; FALLBACK_HINT; the text logic in the component body)
    - .planning/phases/04-practice-engine/04-UI-SPEC.md — FeedbackSlot visual spec (min-h-[48px], text-primary for encouragement, text-lg, aria-live="polite"); Color section: Primary orange (#FF6B35) is text-primary, Accent blue (#3B82F6) is text-accent
  </read_first>
  <behavior>
    - FeedbackSlot renders a div with min-h-[48px] always (reserves space, preventing layout shift per D-08)
    - When attemptCount === 0: div is empty — aria-live region is present but no child p element rendered (height reserved, reader announces nothing)
    - When attemptCount === 1: p element with the encouragement phrase at phraseIndex % 5; className="text-lg text-primary text-center" (orange — warm, non-alarming)
    - When attemptCount >= 2: p element with FALLBACK_HINT text; className="text-lg text-accent text-center font-semibold" (blue — signals guidance, distinct from encouragement orange)
    - At no point are both a phrase and the hint rendered simultaneously — the if/else logic ensures mutual exclusion
    - aria-live="polite" on the container div (screen reader announces text changes without interrupting)
    - ConfettiScreen: buttonLabel?: string prop added; both aria-label and visible text use buttonLabel ?? 'Start Practice'
    - LessonScreen.test.tsx still passes after ConfettiScreen change (default value preserves 'Start Practice')
  </behavior>
  <action>
    Create src/components/FeedbackSlot.tsx:
    - Export interface FeedbackSlotProps: { attemptCount: number; phraseIndex: number }
    - const ENCOURAGEMENT_PHRASES: string[] array with exactly these 5 entries in order: "Nice try! Give it another go.", "Almost! You can do it.", "Keep going — you're doing great!", "Not quite, but don't give up!", "Hmm, try again!"
    - const FALLBACK_HINT = "Think about what number makes the equation balance."
    - export function FeedbackSlot({ attemptCount, phraseIndex }: FeedbackSlotProps)
    - Compute text and style via if/else — mutually exclusive branches:
      if attemptCount === 0: text = null, textClass = ''
      else if attemptCount === 1: text = ENCOURAGEMENT_PHRASES[phraseIndex % ENCOURAGEMENT_PHRASES.length], textClass = "text-lg text-primary text-center"
      else: text = FALLBACK_HINT, textClass = "text-lg text-accent text-center font-semibold"
    - Return: div className="min-h-[48px] flex items-center justify-center px-6" aria-live="polite"; inside: text !== null && p element with className={textClass} containing the text
    - The container div always renders (height always reserved). The p element renders only when text !== null.

    Modify src/components/ConfettiScreen.tsx:
    - Add buttonLabel?: string to the props interface (after onStartPractice — keep it optional with ?: so Phase 3 callers without it still compile)
    - Destructure buttonLabel from props in the function signature
    - Change button aria-label from the hardcoded string "Start Practice" to {buttonLabel ?? 'Start Practice'}
    - Change button text content from the hardcoded string "Start Practice" to {buttonLabel ?? 'Start Practice'}
    - No other changes to ConfettiScreen — do not touch confetti logic, colors, or layout
  </action>
  <verify>
    <automated>cd /Users/omar/projects/math-tutor && npx vitest run src/screens/LessonScreen.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/FeedbackSlot.tsx exists and exports FeedbackSlot
    - src/components/FeedbackSlot.tsx contains the string "Nice try! Give it another go."
    - src/components/FeedbackSlot.tsx contains FALLBACK_HINT with text "Think about what number makes the equation balance."
    - src/components/FeedbackSlot.tsx contains aria-live="polite"
    - src/components/FeedbackSlot.tsx contains "text-primary" class for encouragement state (attemptCount === 1 branch)
    - src/components/FeedbackSlot.tsx contains "text-accent" class for hint state (attemptCount >= 2 branch)
    - src/components/FeedbackSlot.tsx does NOT use a ternary or logical operator that could render both text-primary and text-accent content simultaneously — the if/else structure ensures mutual exclusion
    - src/components/FeedbackSlot.tsx does NOT render a p element when attemptCount === 0
    - src/components/FeedbackSlot.tsx does NOT contain any min-h value less than 48px
    - src/components/ConfettiScreen.tsx contains `buttonLabel?: string` in the interface
    - src/components/ConfettiScreen.tsx button aria-label is `{buttonLabel ?? 'Start Practice'}` (not hardcoded)
    - `npx vitest run src/screens/LessonScreen.test.tsx` exits 0 — regression passes, 'Start Practice' button still found
  </acceptance_criteria>
  <done>FeedbackSlot created with orange encouragement and blue hint styles that are mutually exclusive; ConfettiScreen extended with buttonLabel prop; LessonScreen regression test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire feedback escalation and session end into PracticeScreen</name>
  <files>src/screens/PracticeScreen.tsx</files>
  <read_first>
    - src/screens/PracticeScreen.tsx — full file as written by Plan 01; find the inline FeedbackSlot placeholder div; find the ConfettiScreen render in the celebration branch; find the reveal timer useEffect; confirm handleCorrectAnswer and handleWrongAnswer both have `if (state.phase !== 'answering') return` guards — do NOT remove them
    - src/components/FeedbackSlot.tsx — props interface (attemptCount, phraseIndex) just created
    - src/components/ConfettiScreen.tsx — updated interface with buttonLabel?: string
    - src/screens/PracticeScreen.test.tsx — FEED-01 through FEED-04 tests and D-03, D-11 tests; implement until all pass
    - .planning/phases/04-practice-engine/04-CONTEXT.md — D-07 (encouragement inline below question), D-08 (hint replaces encouragement, same slot), D-09 (correct answer highlighted green; 1.5s pause; auto-advance), D-10 (reuse ConfettiScreen), D-11 (buttonLabel='Back to Home'), D-12 (navigate('/') on button tap)
    - .planning/phases/04-practice-engine/04-UI-SPEC.md — Copywriting Contract: "The answer is {N}. Moving to the next problem." for correct-reveal aria-live="assertive" announcement
  </read_first>
  <behavior>
    - Replace the inline `<div className="min-h-[48px]" aria-hidden="true" />` placeholder with FeedbackSlot component passing attemptCount={state.attemptCount} phraseIndex={state.phraseIndex}
    - In the celebration branch: ConfettiScreen now receives buttonLabel="Back to Home" in addition to onStartPractice={() => navigate('/')}
    - Add a hidden aria-live="assertive" div to the main render (outside the FeedbackSlot) that announces correct-reveal: when state.phase === 'revealing', render the text "The answer is {currentProblem.answer}. Moving to the next problem." inside this div; when phase is not 'revealing', render an empty string. This uses aria-live="assertive" so it interrupts immediately — distinct from FeedbackSlot's "polite" region.
    - The reveal timer useEffect (already in Plan 01) must be confirmed present and correct: watches state.phase === 'revealing', fires setTimeout 1500ms to dispatch ADVANCE + moves focus to progressRef, clears on cleanup. Do NOT add a second useEffect.
    - The phase guard (if state.phase !== 'answering') return in handleCorrectAnswer and handleWrongAnswer must remain — do not remove or weaken it
    - Attempt counter resets to 0 in ADVANCE action (verified in reducer from Plan 01)
    - FEED-03 test: after 1 wrong answer, screen contains one of the 5 encouragement phrases (orange text-primary in FeedbackSlot)
    - FEED-01 test: after 2 wrong answers on same problem, screen contains FALLBACK_HINT text (blue text-accent in FeedbackSlot — different from encouragement)
    - D-03 test: after 3 wrong answers (reveal + advance), the next problem starts with attemptCount 0 (FeedbackSlot shows nothing — empty div, no p element)
  </behavior>
  <action>
    Open src/screens/PracticeScreen.tsx. Make the following targeted changes — do NOT rewrite the whole file:

    1. Add import: import { FeedbackSlot } from '../components/FeedbackSlot' at the top imports section.

    2. Locate the inline placeholder div (className="min-h-[48px]" aria-hidden="true"). Replace it with:
       `<FeedbackSlot attemptCount={state.attemptCount} phraseIndex={state.phraseIndex} />`

    3. Locate the celebration branch ConfettiScreen render. Add the buttonLabel prop:
       buttonLabel="Back to Home"
       Confirm onStartPractice={() => navigate('/')} is already present.

    4. In the main render (the 'answering' | 'revealing' branch), add a visually hidden aria-live="assertive" region immediately before or after the FeedbackSlot. This element must:
       - Always be present in the DOM (not conditionally rendered) so the aria-live region is established before it needs to fire
       - Have className="sr-only" (Tailwind screen-reader-only utility) or equivalent: position absolute; width: 1px; height: 1px; overflow: hidden; clip: rect(0,0,0,0); whiteSpace: nowrap
       - Contain: state.phase === 'revealing' ? `The answer is ${currentProblem.answer}. Moving to the next problem.` : ''
       - Have aria-live="assertive" aria-atomic="true"
       - Do NOT replace FeedbackSlot — this is a separate, hidden element

    5. Confirm the useEffect for the reveal timer is present and correctly structured:
       - Dependency array includes state.phase (and optionally problemPool.length)
       - Returns () => clearTimeout(timer)
       - Dispatches { type: 'ADVANCE', totalProblems: problemPool.length } and calls progressRef.current?.focus()
       If it is missing or incomplete, add or fix it now.

    6. Confirm handleCorrectAnswer and handleWrongAnswer still have `if (state.phase !== 'answering') return` as their first statement. Do not remove these guards.

    7. Do not change the reducer, the widget components, the progress dots, or the screen container dimensions. Only the changes above are needed.

    After changes, run the full PracticeScreen test suite. All FEED-01, FEED-02, FEED-03, FEED-04, D-03, and D-11 tests must pass.
  </action>
  <verify>
    <automated>cd /Users/omar/projects/math-tutor && npx vitest run src/screens/PracticeScreen.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/screens/PracticeScreen.tsx imports FeedbackSlot from ../components/FeedbackSlot
    - src/screens/PracticeScreen.tsx contains `buttonLabel="Back to Home"` in the ConfettiScreen render
    - src/screens/PracticeScreen.tsx does NOT contain the inline placeholder `aria-hidden="true"` div that was the FeedbackSlot placeholder (it has been replaced by the FeedbackSlot component)
    - src/screens/PracticeScreen.tsx contains an aria-live="assertive" region with the correct-reveal announcement text pattern matching `The answer is`
    - src/screens/PracticeScreen.tsx still contains `if (state.phase !== 'answering') return` in handleCorrectAnswer and handleWrongAnswer (guards not removed)
    - FEED-01 test passes: after 2 wrong answers, hint text "Think about what number makes the equation balance." is in the document
    - FEED-02 test passes: after 3 wrong answers + vi.advanceTimersByTime(1500), next problem or ConfettiScreen is shown
    - FEED-03 test passes: after 1 wrong answer, one of the 5 encouragement phrases is in the document
    - FEED-04 test passes: after all problems answered, ConfettiScreen with "Back to Home" button is shown
    - D-03 test passes: attempt counter resets per problem (FeedbackSlot is empty at start of new problem — no p element in the slot div)
    - D-11 test passes: LessonScreen ConfettiScreen still shows 'Start Practice'
    - `npx vitest run src/screens/PracticeScreen.test.tsx` exits 0 for ALL tests
    - `npx vitest run` (full suite) exits 0
  </acceptance_criteria>
  <done>FeedbackSlot wired into PracticeScreen with correct props; hint and encouragement visually distinct and never overlapping; aria-live="assertive" region announces correct answer on reveal; ConfettiScreen shows 'Back to Home'; phase guards preserved; all FEED requirements covered by passing tests; full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PracticeState → FeedbackSlot | attemptCount and phraseIndex are in-memory integers; no external input |
| celebration phase → navigation | navigate('/') is a client-side router call; no URL injection surface |
| reveal announcement → aria-live | String interpolation of problem.answer (a number from static build-time data); no user-controlled input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | phraseIndex modulo | accept | phraseIndex % ENCOURAGEMENT_PHRASES.length is a safe array access; array is a compile-time constant with exactly 5 entries; no out-of-bounds possible |
| T-04-02-02 | Denial of Service | reveal timer (setTimeout 1500ms) | mitigate | useEffect returns clearTimeout(timer) on cleanup; timer cannot fire after component unmounts; only one useEffect watches state.phase === 'revealing' — a second is not added in this plan |
| T-04-02-03 | Elevation of Privilege | buttonLabel prop on ConfettiScreen | accept | buttonLabel is a string literal set in source code; no user-controlled input reaches this prop |
| T-04-02-04 | Tampering | FeedbackSlot simultaneous message display | mitigate | if/else logic in FeedbackSlot is mutually exclusive — only one p element rendered per render cycle; no ternary or logical operator that could render both text-primary and text-accent simultaneously |
| T-04-02-05 | Tampering | aria-live="assertive" injection | accept | Announcement string is `The answer is ${currentProblem.answer}` where currentProblem.answer is a number from static build-time curriculum data; no user-controlled string enters the aria region |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages in Phase 4 — confirmed by RESEARCH.md Package Legitimacy Audit |
</threat_model>

<verification>
After both tasks complete:

1. `npx vitest run src/screens/PracticeScreen.test.tsx` — all PRAC-* and FEED-* tests pass
2. `npx vitest run src/screens/LessonScreen.test.tsx` — no regression (D-11 verified)
3. `npx vitest run` — full suite exits 0
4. Grep: `grep "text-primary" src/components/FeedbackSlot.tsx` — returns match in encouragement branch
5. Grep: `grep "text-accent" src/components/FeedbackSlot.tsx` — returns match in hint branch
6. Grep: `grep "aria-live=\"assertive\"" src/screens/PracticeScreen.tsx` — returns match
7. Grep: `grep "if (state.phase !== 'answering') return" src/screens/PracticeScreen.tsx` — returns 2 matches (one per callback)

Manual checks (cannot be automated in jsdom):
- Open on iPad Safari: tap digit keys — no software keyboard appears
- Navigate through a full session — ConfettiScreen appears with "Back to Home" label
- Tap "Back to Home" — navigates to "/" (HomeScreen)
- Submit 1 wrong answer — orange "Nice try!" text appears below question
- Submit 2nd wrong answer — blue hint text "Think about what number..." replaces orange text in same position; no orange text visible
- During 1.5s reveal window — tapping any card or digit key does nothing
</verification>

<success_criteria>
- After 1 wrong answer: orange encouragement phrase (text-primary) visible below the problem in FeedbackSlot
- After 2 wrong answers on same problem: blue hint text (text-accent) visible in same slot — orange text is gone, not alongside it
- After 3 wrong answers: correct answer shown (green in widget); aria-live="assertive" announces "The answer is N. Moving to the next problem."; 1.5s later app advances without any tap
- During any transition window (200ms or 1.5s): taps are silently ignored by both widget guards and PracticeScreen callback guards
- After last problem: ConfettiScreen with "Back to Home" button appears
- Tapping "Back to Home" returns to HomeScreen
- LessonScreen's ConfettiScreen still shows "Start Practice" (no regression)
- Full vitest suite exits 0
</success_criteria>

<output>
Create `.planning/phases/04-practice-engine/04-02-SUMMARY.md` when done
</output>
