---
phase: 04-practice-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/screens/LessonScreen.tsx
  - src/screens/PracticeScreen.tsx
  - src/screens/PracticeScreen.test.tsx
  - src/screens/App.test.tsx
  - src/components/MultipleChoiceWidget.tsx
  - src/components/DigitGridWidget.tsx
  - src/index.css
autonomous: true
requirements: [PRAC-01, PRAC-02, PRAC-03]

must_haves:
  truths:
    - "Child navigates from lesson end to /practice/:lessonId and sees a real problem (not a stub)"
    - "Multiple-choice problem displays 4 cards with shuffled choices; tapping a correct card immediately registers the answer"
    - "Digit-grid problem displays a composed-answer display box and 3x4 key grid; tapping digits appends to display, no software keyboard appears"
    - "Backspace removes the last composed digit; Check Answer button is disabled when display is empty"
    - "Progress dots at the top show problem 1 of N correctly"
    - "No additional input is accepted during the 200ms CORRECT_ANSWER or 1.5s REVEAL windows — taps during transitions are silently ignored"
    - "Digit composition is capped at 3 digits — a child cannot compose an unrealistic answer (e.g. 9999)"
  artifacts:
    - path: "src/screens/PracticeScreen.tsx"
      provides: "useReducer state machine: practiceReducer, PracticeState, PracticeAction"
      contains: "useParams"
    - path: "src/components/MultipleChoiceWidget.tsx"
      provides: "2x2 grid of answer cards, tap-to-submit with phase guard"
      exports: ["MultipleChoiceWidget"]
    - path: "src/components/DigitGridWidget.tsx"
      provides: "digit display box + 3x4 key grid, no input element, digit cap enforced"
      exports: ["DigitGridWidget"]
    - path: "src/screens/PracticeScreen.test.tsx"
      provides: "test scaffold covering PRAC-01, PRAC-02, and input-lock behaviors"
      contains: "renderWithRoute"
    - path: "src/index.css"
      provides: "wrong-pulse and correct-flash keyframes"
      contains: "wrong-pulse"
  key_links:
    - from: "src/App.tsx"
      to: "src/screens/PracticeScreen.tsx"
      via: "Route path='/practice/:lessonId'"
      pattern: "practice/:lessonId"
    - from: "src/screens/LessonScreen.tsx"
      to: "/practice/:lessonId"
      via: "navigate(`/practice/${lesson.id}`)"
      pattern: "navigate.*practice.*lesson\\.id"
    - from: "src/screens/PracticeScreen.tsx"
      to: "src/curriculum/index"
      via: "problems.filter(p => p.lessonId === lessonId)"
      pattern: "problems\\.filter"
---

<objective>
Replace the PracticeScreen stub with a working practice engine: route plumbing, state machine, and both input widgets. After this plan a child can navigate from the lesson celebration screen to a real practice problem, answer it (multiple-choice or digit-grid), and see progress advance.

Purpose: Delivers the complete happy-path vertical slice for PRAC-01, PRAC-02, PRAC-03. All feedback escalation (wrong answers, hints, reveal, session end) is wired in Plan 02.

Output: PracticeScreen state machine, MultipleChoiceWidget, DigitGridWidget, route fix, test scaffold, CSS keyframes.

Review notes applied (from 04-REVIEWS.md):
- HIGH: Input locking — both widgets guard against taps when phase !== 'answering'
- HIGH (digit cap): composedDigits capped at MAX_DIGITS = 3 (covers all grades 1–3 answers)
- MEDIUM: DigitGridWidget keys carry explicit ARIA role descriptions; focus management on problem advance
- MEDIUM: MultipleChoiceWidget early-return guard on onClick in addition to disabled prop
- LOW: Dev-only console.warn when problem pool is empty
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-practice-engine/04-CONTEXT.md
@.planning/phases/04-practice-engine/04-RESEARCH.md
@.planning/phases/04-practice-engine/04-PATTERNS.md
@.planning/phases/04-practice-engine/04-UI-SPEC.md
</context>

<interfaces>
From src/curriculum/types.ts (excerpt — read the full file before implementing):
  type Topic = 'addition' | 'subtraction' | 'word-problem'
  interface BaseProblem { id: string; lessonId: string; grade: 1|2|3; topic: Topic; question: string; answer: number }
  interface MultipleChoiceProblem extends BaseProblem { type: 'multiple-choice'; choices: number[] }
  interface DigitGridProblem extends BaseProblem { type: 'digit-grid' }
  type Problem = MultipleChoiceProblem | DigitGridProblem

From src/screens/LessonScreen.tsx (state machine pattern to mirror exactly):
  type LessonPhase = 'unlock' | 'playing' | 'between-steps' | 'celebration'
  interface LessonState { phase: LessonPhase; stepIndex: number }
  type LessonAction = { type: 'UNLOCK' } | { type: 'AUDIO_ENDED' } | { type: 'ADVANCE'; totalSteps: number } | { type: 'REPLAY' }
  function lessonReducer(state, action): LessonState { switch(action.type) { ... default: return state } }
  export default function LessonScreen() {
    const { lessonId } = useParams<{ lessonId?: string }>()
    const [state, dispatch] = useReducer(lessonReducer, { phase: 'unlock', stepIndex: 0 })
    const navigate = useNavigate()
    ...
  }

From src/App.tsx (current route — must change):
  <Route path="/practice" element={<PracticeScreen />} />  // change to /practice/:lessonId

From src/components/StepCard.tsx (card + button pattern to follow):
  className="flex flex-col bg-white rounded-2xl shadow-sm p-8 w-4/5 max-w-lg"
  button: className="... min-h-[44px] min-w-[44px] flex items-center justify-center" style={{ touchAction: 'manipulation' }}

From src/index.css @theme (color tokens):
  --color-primary: #FF6B35  (bg-primary, text-primary, border-primary)
  --color-accent: #3B82F6   (bg-accent — Check Answer button)
  --color-success: #22C55E  (bg-success, border-success, text-success)
  --color-surface: #FFF8F0  (bg-surface — screen background)
  --color-on-surface: #1C1917 (text-on-surface)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route wiring and test scaffold</name>
  <files>src/App.tsx, src/screens/LessonScreen.tsx, src/screens/App.test.tsx, src/screens/PracticeScreen.test.tsx</files>
  <read_first>
    - src/App.tsx — current route definitions (line 12: /practice has no param)
    - src/screens/LessonScreen.tsx — line 111: navigate('/practice') call to update; line 41: useParams pattern to mirror
    - src/screens/App.test.tsx — lines 25-30: existing PracticeScreen routing test to update
    - src/curriculum/index.ts — problems export shape; first lessonId value in data (use 'addition-grade1-01' as the canonical test lessonId)
  </read_first>
  <behavior>
    - App.tsx route for practice changes from path="/practice" to path="/practice/:lessonId"
    - LessonScreen navigate('/practice') changes to navigate(`/practice/${lesson.id}`)
    - App.test.tsx test 'renders PracticeScreen at /practice' updates to route /practice/addition-grade1-01 and asserts screen.getByLabelText(/problem 1 of/i)
    - PracticeScreen.test.tsx is created with: vi.useFakeTimers() in beforeEach, vi.useRealTimers() in afterEach, renderWithRoute helper using MemoryRouter + Routes + Route path="/practice/:lessonId", and describe blocks for all behaviors including input-lock during transitions
    - Input-lock tests: assert that tapping a card when phase === 'revealing' does NOT advance the problem index (i.e. dispatch is not called again during transition window)
  </behavior>
  <action>
    In src/App.tsx: change `path="/practice"` to `path="/practice/:lessonId"`. No other changes.

    In src/screens/LessonScreen.tsx: change line 111's `navigate('/practice')` to `navigate(\`/practice/${lesson.id}\`)`. The lesson variable is already in scope at that line. No other changes.

    In src/screens/App.test.tsx: update the test that renders PracticeScreen. Change initialEntries from ['/practice'] to ['/practice/addition-grade1-01']. Change the assertion from getByText('Practice') to getByLabelText(/problem 1 of/i). Update the test description to 'renders PracticeScreen at /practice/:lessonId'.

    Create src/screens/PracticeScreen.test.tsx with the following structure:
    - Imports: describe, it, expect, vi, beforeEach, afterEach from vitest; render, screen, fireEvent, act from @testing-library/react; MemoryRouter, Route, Routes from react-router; PracticeScreen from ./PracticeScreen; problems from ../curriculum/index
    - renderWithRoute(lessonId: string) helper: renders MemoryRouter initialEntries=[`/practice/${lessonId}`] wrapping Routes > Route path="/practice/:lessonId" element={PracticeScreen}
    - beforeEach: vi.useFakeTimers(); afterEach: vi.useRealTimers()
    - Describe blocks with concrete tests for:
      PRAC-01 (4 cards rendered for a grade-1 multiple-choice lesson)
      PRAC-01 tap-to-submit (correct answer advances problem index)
      PRAC-01 input-lock: tapping any card when phase is 'revealing' (during 200ms window) does NOT fire a second onSubmit — test by rendering a mc problem, submitting correct answer, then immediately tapping another card, asserting the problem question text has not changed twice (or that attempt count is still 0 on next problem)
      PRAC-02 (no input element in DOM for a digit-grid lesson)
      PRAC-02 digit composition: tapping digit '3' shows '3' in the aria-label region
      PRAC-02 digit cap: tapping 4 digit keys only composes 3 digits (the 4th tap is ignored)
      PRAC-02 backspace: tapping backspace after composing '5' shows empty state
      PRAC-02 Check disabled when empty
      FEED-01 hint after 2 wrong answers (FEED tests may be .todo if FeedbackSlot not yet present)
      FEED-02 reveal after 3 wrong answers + vi.advanceTimersByTime(1500)
      FEED-03 encouragement phrase after 1st wrong answer
      FEED-04 ConfettiScreen shown after all problems answered
      D-03 attempt counter resets after advance (FeedbackSlot empty at start of next problem)
      D-11 LessonScreen ConfettiScreen regression (still shows 'Start Practice')

    Use 'addition-grade1-01' as the default lessonId for multiple-choice tests (grade 1). For digit-grid tests, find a grade-2 or grade-3 lessonId from the problems import where problem.type === 'digit-grid'.
  </action>
  <verify>
    <automated>cd /Users/omar/projects/math-tutor && npx vitest run src/screens/App.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx contains `path="/practice/:lessonId"` (not `path="/practice"`)
    - src/screens/LessonScreen.tsx contains navigate with template literal including lesson.id for the practice route
    - src/screens/App.test.tsx test for PracticeScreen uses initialEntries={['/practice/addition-grade1-01']} and asserts getByLabelText(/problem 1 of/i)
    - src/screens/PracticeScreen.test.tsx exists and compiles without TypeScript errors
    - src/screens/PracticeScreen.test.tsx contains a test description matching /input.lock/i or /ignores tap during/i or /phase.*revealing/i
    - src/screens/PracticeScreen.test.tsx contains a test description matching /digit.*cap/i or /max.*digit/i or /3 digit/i
    - `npx vitest run src/screens/App.test.tsx` exits 0
    - `npx vitest run src/screens/LessonScreen.test.tsx` exits 0 (regression: LessonScreen confetti button still says "Start Practice")
  </acceptance_criteria>
  <done>Route is /practice/:lessonId; LessonScreen passes lessonId in navigate; App.test.tsx passes; LessonScreen.test.tsx passes; PracticeScreen.test.tsx file exists, compiles, and includes input-lock and digit-cap test stubs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PracticeScreen state machine</name>
  <files>src/screens/PracticeScreen.tsx</files>
  <read_first>
    - src/screens/LessonScreen.tsx — full file; this is the exact structural analog to mirror
    - src/curriculum/index.ts — problems export; lessonId field
    - src/curriculum/types.ts — Problem discriminated union; MultipleChoiceProblem; DigitGridProblem
    - src/screens/PracticeScreen.test.tsx — test expectations already written; implement to make them pass
  </read_first>
  <behavior>
    - PracticeScreen mounts with useParams lessonId, filters problems by lessonId, shuffles the pool once via useMemo([lessonId])
    - State machine: PracticePhase = 'answering' | 'revealing' | 'celebration'; PracticeState has { phase, problemIndex, attemptCount, composedDigits: string, phraseIndex: number }; initial state { phase: 'answering', problemIndex: 0, attemptCount: 0, composedDigits: '', phraseIndex: 0 }
    - PracticeAction union: CORRECT_ANSWER | WRONG_ANSWER | ADVANCE({ totalProblems }) | COMPOSE_DIGIT({ digit: string }) | BACKSPACE | BEGIN_REVEAL
    - ADVANCE action: if problemIndex + 1 >= totalProblems → phase: 'celebration'; else → phase: 'answering', problemIndex: next, attemptCount: 0, composedDigits: '', phraseIndex unchanged
    - WRONG_ANSWER action: attemptCount + 1, composedDigits: '', phraseIndex + 1
    - COMPOSE_DIGIT action: if composedDigits.length >= MAX_DIGITS (3) return state unchanged; else composedDigits + digit
    - BACKSPACE action: composedDigits.slice(0, -1)
    - BEGIN_REVEAL action: phase → 'revealing'
    - Input lock enforcement in PracticeScreen's onSubmit/onWrong callbacks: add guard `if (state.phase !== 'answering') return` at the top of each handler function. This is the primary lock — widget props (disabled, onClick guard) are a secondary UX layer.
    - CORRECT_ANSWER dispatched from onSubmit callback: only when state.phase === 'answering'. After dispatch, schedule a 200ms setTimeout to dispatch ADVANCE. Store timeout ref for cleanup.
    - WRONG_ANSWER dispatched from onWrong callback: only when state.phase === 'answering'. After dispatch, check if the resulting attemptCount (state.attemptCount + 1) >= 3; if so also dispatch BEGIN_REVEAL immediately.
    - When phase is 'celebration': return ConfettiScreen (Plan 02 wires buttonLabel; for now pass onStartPractice={() => navigate('/')} and buttonLabel="Back to Home")
    - When phase is 'answering' or 'revealing': render full-screen container, progress dots, problem question text, FeedbackSlot placeholder (empty div for now — Plan 02 fills it), then MultipleChoiceWidget or DigitGridWidget based on problem.type
    - Choices for multiple-choice problems are shuffled via useMemo keyed on currentProblem.id
    - The reveal timer useEffect watches state.phase === 'revealing', fires setTimeout 1500ms → dispatch ADVANCE, with clearTimeout cleanup
    - Progress dots container: aria-label="Problem {problemIndex + 1} of {problemPool.length}" on the container div; 8px dots using inline style backgroundColor
    - Problem question: className="text-2xl font-bold text-on-surface text-center px-6" + user-select: none inline style
    - Full-screen container: className="flex flex-col items-center gap-6 bg-surface w-full" style={{ height: '100dvh', paddingTop: 'env(safe-area-inset-top)', paddingBottom: 'env(safe-area-inset-bottom)' }}
    - If problemPool is empty: if (import.meta.env.DEV) console.warn('[PracticeScreen] No problems found for lessonId:', lessonId); then immediately render ConfettiScreen (silent fallback per UI-SPEC)
    - After problem advances (ADVANCE action resolves to next problem), move focus to the progress dots container using a ref so screen readers announce the new problem position
  </behavior>
  <action>
    Replace all content in src/screens/PracticeScreen.tsx. The file currently contains a 4-line stub.

    Define at the module level before the component: const MAX_DIGITS = 3

    Structure the file in this order:
    1. Imports: useReducer, useEffect, useMemo, useRef from react; useNavigate, useParams from react-router; ConfettiScreen from ../components/ConfettiScreen; MultipleChoiceWidget from ../components/MultipleChoiceWidget; DigitGridWidget from ../components/DigitGridWidget; problems from ../curriculum/index; type Problem from ../curriculum/types
    2. const MAX_DIGITS = 3
    3. PracticePhase type alias
    4. PracticeState interface
    5. PracticeAction type union
    6. practiceReducer function — pure, switch on action.type; COMPOSE_DIGIT case guards composedDigits.length >= MAX_DIGITS with early return state; default: return state
    7. export default function PracticeScreen():
       a. useParams, useMemo problem pool with DEV console.warn on empty, useReducer, useNavigate
       b. progressRef = useRef<HTMLDivElement>(null) for focus management
       c. correctAnswerTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null)
       d. handleCorrectAnswer callback: if (state.phase !== 'answering') return; dispatch CORRECT_ANSWER; set correctAnswerTimerRef.current = setTimeout(() => { dispatch ADVANCE; progressRef.current?.focus() }, 200)
       e. handleWrongAnswer callback: if (state.phase !== 'answering') return; dispatch WRONG_ANSWER; if (state.attemptCount + 1 >= 3) dispatch BEGIN_REVEAL
       f. handleDigit callback: dispatch COMPOSE_DIGIT (reducer enforces cap)
       g. handleBackspace callback: dispatch BACKSPACE
       h. handleCheckAnswer callback: if (state.phase !== 'answering') return; const isCorrect = Number(state.composedDigits) === currentProblem.answer; if isCorrect: handleCorrectAnswer(); else: handleWrongAnswer()
       i. useEffect for reveal timer: watches state.phase === 'revealing'; setTimeout 1500ms → dispatch ADVANCE + progressRef.current?.focus(); clearTimeout cleanup
       j. useEffect cleanup for correctAnswerTimerRef on unmount
       k. Shuffle choices useMemo keyed on currentProblem?.id
       l. Celebration branch early return
       m. Empty pool early return (with DEV warn already called above during memo)
       n. Main render: full-screen container, progress dots div with ref={progressRef} tabIndex={-1} and aria-label, question p, inline FeedbackSlot placeholder div, widget switch on problem.type

    The FeedbackSlot placeholder is: `<div className="min-h-[48px]" aria-hidden="true" />`  — Plan 02 replaces this with the FeedbackSlot component.

    Add comment: // TODO Phase 5: add hint field to BaseProblem and curriculum.json for per-problem hints
  </action>
  <verify>
    <automated>cd /Users/omar/projects/math-tutor && npx vitest run src/screens/PracticeScreen.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/screens/PracticeScreen.tsx contains `useParams` import from 'react-router'
    - src/screens/PracticeScreen.tsx contains `practiceReducer` function
    - src/screens/PracticeScreen.tsx contains `problems.filter` for lessonId
    - src/screens/PracticeScreen.tsx contains `useMemo` with lessonId in dependency array for problem pool
    - src/screens/PracticeScreen.tsx contains `MAX_DIGITS = 3`
    - src/screens/PracticeScreen.tsx contains `if (state.phase !== 'answering') return` in handleCorrectAnswer and handleWrongAnswer
    - src/screens/PracticeScreen.tsx contains `import.meta.env.DEV` guard around console.warn
    - src/screens/PracticeScreen.tsx contains `progressRef` and `tabIndex={-1}` on progress dots container for focus management
    - src/screens/PracticeScreen.tsx does NOT contain any `<input` element
    - src/screens/PracticeScreen.tsx does NOT import from 'react-router-dom'
    - PRAC-01 tests pass: 4 cards rendered, tap correct card advances problem
    - PRAC-02 tests pass: no input in DOM, digit compose works, digit cap at 3 works, backspace works, Check disabled when empty
    - Input-lock test passes: tapping during revealing phase does not re-dispatch
    - `npx vitest run src/screens/PracticeScreen.test.tsx` exits 0 for all PRAC-* tests (FEED tests may be pending/todo at this stage)
  </acceptance_criteria>
  <done>PracticeScreen renders real problems from curriculum filtered by lessonId; state machine transitions correctly; input is locked when phase !== 'answering'; digit cap is enforced at 3; both widget types render; PRAC-01 and PRAC-02 tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: MultipleChoiceWidget, DigitGridWidget, and CSS animation keyframes</name>
  <files>src/components/MultipleChoiceWidget.tsx, src/components/DigitGridWidget.tsx, src/index.css</files>
  <read_first>
    - src/components/StepCard.tsx — card shell pattern (bg-white rounded-2xl shadow-sm), button pattern with touchAction, inline SVG convention with stroke="#FF6B35"
    - src/index.css — existing @keyframes confetti-fall (lines 48-57); @theme block; global button rule (min-height/min-width: 44px, touch-action: manipulation)
    - src/components/ConfettiScreen.tsx — button sizing pattern (min-h-[64px], bg-accent text-white)
    - .planning/phases/04-practice-engine/04-PATTERNS.md — MultipleChoiceWidget props interface; DigitGridWidget props interface; digit grid key layout [7][8][9]/[4][5][6]/[1][2][3]/[⌫][0][Check]
    - .planning/phases/04-practice-engine/04-UI-SPEC.md — color assignments for each state; digit display box specs; wrong-answer pulse vs correct flash
  </read_first>
  <action>
    Create src/components/MultipleChoiceWidget.tsx:
    - Props interface: { choices: number[], correctAnswer: number, phase: 'answering' | 'revealing', onSubmit: (choice: number) => void }
    - Render: div with role="group" aria-label="Answer choices" className="grid grid-cols-2 gap-4 w-full px-6"
    - Each card: button, key={choice}, aria-label={`Answer ${choice}`}, className including bg-white rounded-2xl shadow-sm min-h-[64px] flex items-center justify-center text-4xl font-bold text-on-surface active:opacity-80; when phase==='revealing' && choice===correctAnswer add border-2 border-success bg-success/10; disabled={phase === 'revealing'}
    - style={{ touchAction: 'manipulation' }} on every button
    - onClick handler: add early-return guard `if (phase !== 'answering') return` as the first line before calling onSubmit(choice). This is a second-layer guard — the disabled prop is the primary UX layer; this guard prevents stale closure race conditions.
    - No internal state; no useMemo inside widget (parent handles shuffle)

    Create src/components/DigitGridWidget.tsx:
    - Props interface: { composedDigits: string, correctAnswer: number, phase: 'answering' | 'revealing', onDigit: (digit: string) => void, onBackspace: () => void, onSubmit: () => void }
    - Display box: div (NOT input), role="status", aria-live="polite", aria-label={composedDigits ? `Your answer: ${composedDigits}` : 'Enter your answer'}, aria-atomic="true", className="bg-white rounded-2xl shadow-sm min-h-[64px] min-w-[120px] flex items-center justify-center text-4xl font-bold"; text color: phase==='revealing' ? 'text-success' : composedDigits ? 'text-on-surface' : 'text-on-surface/30'
    - Display value: phase==='revealing' ? String(correctAnswer) : composedDigits || '_'
    - Key grid: div role="group" aria-label="Number pad" className="grid grid-cols-3 gap-4"
    - Digit keys 7,8,9,4,5,6,1,2,3 (in that order for row layout): button, aria-label={`Digit ${d}`}, aria-description="Tap to add this digit to your answer", className="bg-white rounded-xl shadow-sm min-h-[64px] min-w-[64px] text-2xl font-bold text-on-surface active:opacity-80", style={{ touchAction: 'manipulation' }}, onClick handler: first line `if (phase !== 'answering') return`; then onDigit(d); disabled={phase === 'revealing'}
    - Bottom row position 0 (backspace): button aria-label="Delete last digit", aria-description="Remove the last digit you entered", className="bg-white rounded-xl shadow-sm min-h-[64px] min-w-[64px] flex items-center justify-center active:opacity-80", style={{ touchAction: 'manipulation' }}, disabled={phase === 'revealing'}, onClick handler: first line `if (phase !== 'answering') return`; then onBackspace(); child is inline SVG width=24 height=24 viewBox="0 0 24 24" fill="none" stroke="#FF6B35" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true" with paths: M21 4H8l-7 8 7 8h13a2 2 0 0 0 2-2V6a2 2 0 0 0-2-2z (outer shape), M18 9l-6 6 (diagonal 1), M12 9l6 6 (diagonal 2)
    - Bottom row position 1 (0 digit): same digit key pattern, aria-label="Digit 0", aria-description="Tap to add this digit to your answer", text "0"
    - Bottom row position 2 (Check): button aria-label="Check my answer", aria-description="Submit your answer", className="bg-accent text-white rounded-xl shadow-sm min-h-[64px] text-base font-bold active:opacity-80 disabled:opacity-40", style={{ touchAction: 'manipulation' }}, disabled={composedDigits.length === 0 || phase === 'revealing'}, onClick handler: first line `if (phase !== 'answering') return`; then onSubmit(); text "Check"
    - No input element anywhere in the file

    In src/index.css: add after the existing @keyframes confetti-fall block (around line 57):
      @keyframes wrong-pulse { 0% { border-color: transparent; } 50% { border-color: #FF6B35; } 100% { border-color: transparent; } }
      .wrong-pulse { animation: wrong-pulse 300ms ease-in-out 1; border-width: 2px; border-style: solid; }
      @keyframes correct-flash { 0% { border-color: transparent; } 50% { border-color: #22C55E; background-color: rgb(34 197 94 / 0.1); } 100% { border-color: transparent; background-color: transparent; } }
      .correct-flash { animation: correct-flash 200ms ease-in-out 1; border-width: 2px; border-style: solid; }
  </action>
  <verify>
    <automated>cd /Users/omar/projects/math-tutor && npx vitest run src/screens/PracticeScreen.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/MultipleChoiceWidget.tsx exists and exports MultipleChoiceWidget
    - src/components/DigitGridWidget.tsx exists and exports DigitGridWidget
    - Neither file contains any `<input` element
    - src/components/MultipleChoiceWidget.tsx onClick handler contains `if (phase !== 'answering') return` before calling onSubmit
    - src/components/DigitGridWidget.tsx digit key onClick handlers each contain `if (phase !== 'answering') return` before calling onDigit/onBackspace/onSubmit
    - src/components/DigitGridWidget.tsx contains aria-label="Check my answer"
    - src/components/DigitGridWidget.tsx contains aria-label="Delete last digit"
    - src/components/DigitGridWidget.tsx display box contains role="status" and aria-live="polite"
    - src/components/DigitGridWidget.tsx digit keys contain aria-description or aria-describedby referencing their purpose
    - src/components/MultipleChoiceWidget.tsx wrapper div contains role="group" and aria-label="Answer choices"
    - src/components/DigitGridWidget.tsx key grid contains role="group" and aria-label="Number pad"
    - src/index.css contains `@keyframes wrong-pulse`
    - src/index.css contains `@keyframes correct-flash`
    - `npx vitest run src/screens/PracticeScreen.test.tsx` exits 0 for PRAC-01 and PRAC-02 tests
    - `npx vitest run` (full suite) exits 0 — no regressions
  </acceptance_criteria>
  <done>MultipleChoiceWidget and DigitGridWidget are implemented with phase guards on all tap handlers; no input elements; explicit ARIA roles and descriptions on digit keys; CSS keyframes added; full test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curriculum data → UI | Problems array imported at build time; no runtime fetch; no external input |
| user tap → state | Child taps buttons that dispatch typed actions to a pure reducer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | practiceReducer — ADVANCE action boundary check | mitigate | Reducer validates `next >= totalProblems` before entering celebration phase; no out-of-bounds index access |
| T-04-02 | Denial of Service | useMemo problem pool — empty lessonId param | accept | If lessonId param is absent or matches no problems, pool is empty; DEV console.warn fires; PracticeScreen renders ConfettiScreen immediately (silent fallback) — no infinite loop or crash |
| T-04-03 | Information Disclosure | composedDigits state | accept | In-memory string, never persisted in Phase 4; no PII; resets on each problem advance |
| T-04-04 | Tampering | COMPOSE_DIGIT action — digit cap enforcement | mitigate | Reducer returns state unchanged when composedDigits.length >= MAX_DIGITS (3); prevents unrealistic answer injection |
| T-04-05 | Tampering | Race condition — duplicate dispatch during transition | mitigate | handleCorrectAnswer and handleWrongAnswer guard `if (state.phase !== 'answering') return` as first statement; widget onClick handlers have matching early-return guards as secondary layer |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages in Phase 4 (RESEARCH.md verified: zero new dependencies) |
</threat_model>

<verification>
After all three tasks are complete:

1. `npx vitest run src/screens/PracticeScreen.test.tsx` — all PRAC-01 and PRAC-02 tests pass; input-lock test passes; digit-cap test passes
2. `npx vitest run src/screens/LessonScreen.test.tsx` — no regression (Start Practice button still found)
3. `npx vitest run src/screens/App.test.tsx` — PracticeScreen routing test passes with new route
4. `npx vitest run` — full suite exits 0
5. Grep: `grep -v '^' src/components/DigitGridWidget.tsx | grep -c "input"` — returns 0 (no input elements)
6. Grep: `grep "practice/:lessonId" src/App.tsx` — returns the route line
7. Grep: `grep "wrong-pulse" src/index.css` — returns the keyframe definition
8. Grep: `grep "if (phase !== 'answering') return" src/components/MultipleChoiceWidget.tsx` — returns a match
9. Grep: `grep "MAX_DIGITS" src/screens/PracticeScreen.tsx` — returns the constant definition
10. Grep: `grep "role=\"group\"" src/components/MultipleChoiceWidget.tsx` — returns a match
</verification>

<success_criteria>
- Child can reach /practice/addition-grade1-01 via direct URL and see a real problem question
- Multiple-choice widget renders 4 shuffled answer cards; tapping the correct one advances to the next problem
- Digit-grid widget renders display box + digit keys; composing digits shows them in the box; composing more than 3 digits is silently ignored; Check Answer is disabled when empty; backspace removes last digit
- No software keyboard appears (no input element in DigitGridWidget — enforced by grep)
- Tapping any widget during the 200ms or 1.5s transition window is silently ignored (phase guard in both PracticeScreen callbacks and widget onClick handlers)
- Progress dots reflect current problem position
- Dev console shows a warning when problem pool is empty (not surfaced to child)
- Full vitest suite exits 0
</success_criteria>

<output>
Create `.planning/phases/04-practice-engine/04-01-SUMMARY.md` when done
</output>
