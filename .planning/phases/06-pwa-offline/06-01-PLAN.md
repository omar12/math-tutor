---
phase: 06-pwa-offline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pwa-assets.config.ts
  - public/icons/icon-192.png
  - public/icons/icon-512.png
  - public/icons/icon-512-maskable.png
  - public/icons/apple-touch-icon.png
  - vite.config.ts
  - index.html
  - tsconfig.app.json
  - src/main.tsx
  - src/test/pwa.test.ts
autonomous: true
requirements:
  - PWA-01
  - PWA-02

must_haves:
  truths:
    - "Running `npm run build` produces a dist/ containing a manifest.webmanifest with name 'Math Time!', display 'standalone', theme_color '#FF6B35', and three icon entries"
    - "The service worker precache manifest includes audio/**/*.{mp3,ogg,wav} glob entries and sets navigateFallback to index.html"
    - "public/icons/ contains four PNG files: icon-192.png, icon-512.png, icon-512-maskable.png, apple-touch-icon.png"
    - "index.html contains apple-mobile-web-app-capable, apple-mobile-web-app-status-bar-style, apple-mobile-web-app-title, theme-color, and rel=apple-touch-icon meta/link tags"
    - "src/main.tsx imports registerSW from virtual:pwa-register and calls registerSW({ immediate: true }) before ReactDOM.createRoot"
    - "tsconfig.app.json types array includes vite-plugin-pwa/client"
    - "npx vitest run src/test/pwa.test.ts exits 0 with all assertions passing"
  artifacts:
    - path: "pwa-assets.config.ts"
      provides: "Icon generation config for @vite-pwa/assets-generator"
      contains: "minimal2023Preset"
    - path: "public/icons/icon-192.png"
      provides: "192x192 transparent icon for PWA manifest"
    - path: "public/icons/icon-512-maskable.png"
      provides: "512x512 maskable icon with safe-zone padding"
    - path: "public/icons/apple-touch-icon.png"
      provides: "180x180 PNG for Safari home screen icon"
    - path: "vite.config.ts"
      provides: "VitePWA() plugin with manifest, workbox globPatterns, and navigateFallback"
      contains: "VitePWA"
    - path: "index.html"
      provides: "Apple-specific PWA meta tags"
      contains: "apple-mobile-web-app-capable"
    - path: "src/main.tsx"
      provides: "SW registration for auto-update"
      contains: "registerSW"
    - path: "src/test/pwa.test.ts"
      provides: "Config-level tests for PWA-01 and PWA-02"
  key_links:
    - from: "vite.config.ts (VitePWA manifest)"
      to: "dist/manifest.webmanifest"
      via: "vite build"
      pattern: "VitePWA.*registerType.*autoUpdate"
    - from: "workbox globPatterns"
      to: "dist/sw.js precache manifest"
      via: "Workbox generateSW at build time"
      pattern: "audio/\\*\\*"
    - from: "src/main.tsx registerSW"
      to: "virtual:pwa-register"
      via: "vite-plugin-pwa virtual module"
      pattern: "registerSW.*immediate.*true"
---

<objective>
Wire up the complete PWA infrastructure so the app is installable from iPad Safari and caches all assets for full offline use after the first load.

Purpose: Closes the only remaining offline gap — asset delivery. All data is already local-first (IndexedDB/localStorage). This plan generates the icon PNGs, configures vite-plugin-pwa with the correct manifest and Workbox globPatterns, injects the Apple-specific meta tags into index.html, registers the service worker in main.tsx, and creates config-level tests.

Output: A buildable project whose `dist/` contains a valid manifest, auto-generated service worker, and all required PWA assets.
</objective>

<execution_context>
@/Users/omar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/omar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/omar/projects/math-tutor/.planning/PROJECT.md
@/Users/omar/projects/math-tutor/.planning/ROADMAP.md
@/Users/omar/projects/math-tutor/.planning/phases/06-pwa-offline/06-CONTEXT.md
@/Users/omar/projects/math-tutor/.planning/phases/06-pwa-offline/06-RESEARCH.md
@/Users/omar/projects/math-tutor/.planning/phases/06-pwa-offline/06-PATTERNS.md

<interfaces>
From vite.config.ts (existing plugins array to extend):
```typescript
// Current plugins array (lines 29-33) — VitePWA() appended after audioMissing404Plugin
plugins: [
  react(),
  tailwindcss(),
  audioMissing404Plugin,
  // ADD: VitePWA() here
],
```

From tsconfig.app.json (existing types array to extend — line 7):
```json
"types": ["vite/client", "vitest/globals", "@testing-library/jest-dom"]
// ADD: "vite-plugin-pwa/client" as fourth entry
```

From src/main.tsx (existing imports to prepend before — lines 1-13):
```typescript
// ADD at top, before existing imports:
// import { registerSW } from 'virtual:pwa-register'
// registerSW({ immediate: true })
import React from 'react'
import ReactDOM from 'react-dom/client'
// ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate PNG icons and create pwa-assets.config.ts</name>
  <files>pwa-assets.config.ts, public/icons/icon-192.png, public/icons/icon-512.png, public/icons/icon-512-maskable.png, public/icons/apple-touch-icon.png</files>

  <read_first>
    - /Users/omar/projects/math-tutor/public/favicon.svg — inspect SVG contents to check for CSS variables, external references, or use elements that could break sharp rasterization
    - /Users/omar/projects/math-tutor/package.json — confirm @vite-pwa/assets-generator ^1.0.2 is installed
    - /Users/omar/projects/math-tutor/.planning/phases/06-pwa-offline/06-RESEARCH.md — Architecture Patterns (Pattern 4) and Assumptions A1, A3 for exact generator behavior
  </read_first>

  <action>
    Create pwa-assets.config.ts at the project root (/Users/omar/projects/math-tutor/pwa-assets.config.ts) with this exact content: import defineConfig and minimal2023Preset from '@vite-pwa/assets-generator/config'; export default defineConfig({ preset: minimal2023Preset, images: ['public/favicon.svg'] }).

    Then run the icon generator: `npx pwa-assets-generator` from the project root. The minimal2023Preset generates: icon-192.png (192x192, transparent, purpose: any), icon-512.png (512x512, transparent, purpose: any), icon-512-maskable.png (512x512, white bg + ~10% padding for maskable safe zone, per D-02), and apple-touch-icon.png (180x180, white bg).

    The generator may output icons to public/ root rather than public/icons/. After generation, check where the files landed (ls public/ and ls public/icons/ if it exists). If icons are at public/ root, create the public/icons/ directory and move them there: icon-192.png, icon-512.png, icon-512-maskable.png, apple-touch-icon.png. Per D-03, only public/favicon.svg is used as source — do not use src/assets/.

    After generation, open each PNG and verify it renders the fox mascot correctly (not a blank white square). If the SVG uses CSS variables or external resources that sharp cannot resolve, the output will be blank. In that case: (a) copy public/favicon.svg to public/favicon-icon-source.svg, (b) open it and inline any CSS variable values (#FF6B35 for primary, etc.) directly into the SVG fill/stroke attributes, and (c) rerun the generator pointing at 'public/favicon-icon-source.svg' instead.

    The manifest icon entries in the next task reference paths /icons/icon-192.png, /icons/icon-512.png, /icons/icon-512-maskable.png — confirm the generated files are under public/icons/ to match these paths.
  </action>

  <verify>
    <automated>ls /Users/omar/projects/math-tutor/public/icons/icon-192.png /Users/omar/projects/math-tutor/public/icons/icon-512.png /Users/omar/projects/math-tutor/public/icons/icon-512-maskable.png /Users/omar/projects/math-tutor/public/icons/apple-touch-icon.png && echo "all icons present"</automated>
  </verify>

  <acceptance_criteria>
    - pwa-assets.config.ts exists at project root and contains 'minimal2023Preset'
    - public/icons/icon-192.png exists with file size > 500 bytes (non-blank)
    - public/icons/icon-512.png exists with file size > 500 bytes
    - public/icons/icon-512-maskable.png exists with file size > 500 bytes
    - public/icons/apple-touch-icon.png exists with file size > 500 bytes
    - `file public/icons/icon-192.png` reports 'PNG image data, 192 x 192'
    - `file public/icons/apple-touch-icon.png` reports 'PNG image data, 180 x 180'
  </acceptance_criteria>

  <done>Four PNG icon files exist in public/icons/ and all are non-blank (fox mascot visible). pwa-assets.config.ts exists at project root.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire VitePWA plugin, PWA meta tags, SW registration, TypeScript types, and config tests</name>
  <files>vite.config.ts, index.html, tsconfig.app.json, src/main.tsx, src/test/pwa.test.ts</files>

  <read_first>
    - /Users/omar/projects/math-tutor/vite.config.ts — read full file before editing; preserve audioMissing404Plugin and test section unchanged
    - /Users/omar/projects/math-tutor/index.html — read full file; existing head structure (5 lines) before adding meta tags
    - /Users/omar/projects/math-tutor/tsconfig.app.json — read types array on line 7
    - /Users/omar/projects/math-tutor/src/main.tsx — read full file; registerSW goes above existing imports
    - /Users/omar/projects/math-tutor/src/App.test.tsx — read for vitest describe/it/expect import style convention
    - /Users/omar/projects/math-tutor/.planning/phases/06-pwa-offline/06-RESEARCH.md — Pattern 1 (VitePWA config), Pattern 2 (SW registration), Pattern 3 (index.html tags), Pitfall 3 and 5
    - /Users/omar/projects/math-tutor/.planning/phases/06-pwa-offline/06-PATTERNS.md — all four modification patterns with exact line positions
  </read_first>

  <behavior>
    pwa.test.ts tests:
    - Test 1 (PWA-01): vite.config.ts has a plugin with name containing 'vite-plugin-pwa'
    - Test 2 (PWA-01): the VitePWA manifest object has name === 'Math Time!', short_name === 'Math Time', display === 'standalone', theme_color === '#FF6B35', background_color === '#FFF8F0', start_url === '/', scope === '/'
    - Test 3 (PWA-01): manifest.icons array has entries for sizes '192x192' and '512x512' with purpose 'any', and a '512x512' entry with purpose 'maskable'
    - Test 4 (PWA-02): workbox.globPatterns includes the string 'audio/**/*.{mp3,ogg,wav}'
    - Test 5 (PWA-02): workbox.navigateFallback === 'index.html'
    - Test 6 (PWA-02): workbox.cleanupOutdatedCaches === true
  </behavior>

  <action>
    Write tests first in src/test/pwa.test.ts, run them (they must fail — vite.config.ts does not yet have VitePWA), then implement.

    src/test/pwa.test.ts: import { describe, it, expect } from 'vitest'. Import the default export from '../../vite.config'. The config object has a plugins array. Extract the VitePWA plugin by flattening plugins with .flat() and finding the entry whose name property includes 'vite-plugin-pwa'. Extract the plugin's options (the argument passed to VitePWA()) by accessing the plugin's api or user-options property — or alternatively, export the VitePWA options object from vite.config.ts as a named export `pwaOptions` to make it directly importable in tests without plugin introspection. Use the named export approach (simpler and more reliable than introspection): add `export const pwaOptions = { ... }` in vite.config.ts before defineConfig, then import it in pwa.test.ts as `import { pwaOptions } from '../../vite.config'`.

    vite.config.ts modifications:
    - Add `import { VitePWA } from 'vite-plugin-pwa'` to the import block (after existing imports)
    - Before `export default defineConfig(...)`, add `export const pwaOptions = { ... }` with all VitePWA configuration values (registerType, includeAssets, manifest with all D-08 through D-13 values, workbox with globPatterns covering js/css/html/ico/svg/webmanifest/png/jpg/jpeg/gif/webp and audio/**/*.{mp3,ogg,wav}, navigateFallback: 'index.html', cleanupOutdatedCaches: true)
    - In the plugins array, append `VitePWA(pwaOptions)` after audioMissing404Plugin
    - Leave react(), tailwindcss(), audioMissing404Plugin, and the test section exactly as-is

    index.html modifications (insert after `<meta name="color-scheme" content="light" />` and before `<title>`):
    - `<meta name="theme-color" content="#FF6B35" />` (per D-10)
    - `<meta name="apple-mobile-web-app-capable" content="yes" />` (required for iPadOS standalone — per Pitfall 3 and discretion note)
    - `<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />` (black-translucent allows viewport-fit=cover to work correctly)
    - `<meta name="apple-mobile-web-app-title" content="Math Time" />` (per D-09, no exclamation mark)
    - `<link rel="apple-touch-icon" href="/icons/apple-touch-icon.png" />` (180x180 PNG — per D-01; do NOT use favicon.svg; per PATTERNS.md critical note)
    - Do NOT add `<link rel="manifest">` — vite-plugin-pwa injects this at build time; adding it manually causes a duplicate

    tsconfig.app.json: change the "types" array on line 7 from ["vite/client", "vitest/globals", "@testing-library/jest-dom"] to ["vite/client", "vitest/globals", "@testing-library/jest-dom", "vite-plugin-pwa/client"]. This resolves the TypeScript error for 'virtual:pwa-register'.

    src/main.tsx: prepend two lines at the very top of the file, before the existing React import:
    `import { registerSW } from 'virtual:pwa-register'`
    `registerSW({ immediate: true })`
    All existing imports and the ReactDOM.createRoot call remain unchanged.
  </action>

  <verify>
    <automated>cd /Users/omar/projects/math-tutor && npx vitest run src/test/pwa.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/test/pwa.test.ts` exits 0, all 6 tests pass
    - vite.config.ts contains 'VitePWA' import and 'VitePWA(pwaOptions)' in plugins array
    - vite.config.ts contains `export const pwaOptions` with manifest.name === 'Math Time!' (grep: "Math Time!")
    - vite.config.ts workbox.globPatterns contains 'audio/**/*.{mp3,ogg,wav}'
    - vite.config.ts workbox.navigateFallback === 'index.html'
    - index.html contains `apple-mobile-web-app-capable`
    - index.html contains `apple-mobile-web-app-status-bar-style" content="black-translucent"`
    - index.html contains `apple-touch-icon" href="/icons/apple-touch-icon.png"`
    - index.html does NOT contain `<link rel="manifest"` (vite-plugin-pwa injects it at build time)
    - tsconfig.app.json types array includes "vite-plugin-pwa/client"
    - src/main.tsx line 1 is `import { registerSW } from 'virtual:pwa-register'`
    - src/main.tsx contains `registerSW({ immediate: true })`
    - `npx vitest run` (full suite) exits 0 — no regressions
  </acceptance_criteria>

  <done>All 6 pwa.test.ts assertions pass. Full Vitest suite passes with no regressions. All five files modified correctly per acceptance criteria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SW cache → app shell | Service worker serves cached assets to the app; stale assets could serve outdated code |
| Build pipeline → dist/ | Workbox precache manifest is generated at build time; incorrect globPatterns silently omit files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Service worker scope | mitigate | scope: '/' in manifest limits SW interception to app root only; no cross-origin resource caching configured |
| T-06-02 | Information Disclosure | Stale cached assets after deploy | mitigate | cleanupOutdatedCaches: true removes outdated precache entries on SW update; registerType: 'autoUpdate' forces immediate activation |
| T-06-03 | Tampering | Third-party CDN resources | accept | App has no third-party CDN dependencies in the critical path; no runtime caching of external origins configured |
| T-06-SC | Tampering | npm package installs | accept | All packages (vite-plugin-pwa, @vite-pwa/assets-generator, sharp, workbox-window) received [OK] slopcheck verdict per RESEARCH.md Package Legitimacy Audit; packages already installed in package.json |
</threat_model>

<verification>
Full suite: `npx vitest run` — must pass with 0 failures (≥192 tests from prior phases + 6 new pwa tests).

Config assertions:
```bash
grep -c "VitePWA" /Users/omar/projects/math-tutor/vite.config.ts        # >= 1
grep -c "audio/\*\*" /Users/omar/projects/math-tutor/vite.config.ts     # >= 1
grep -c "navigateFallback" /Users/omar/projects/math-tutor/vite.config.ts # >= 1
grep -c "apple-mobile-web-app-capable" /Users/omar/projects/math-tutor/index.html # >= 1
grep -c "registerSW" /Users/omar/projects/math-tutor/src/main.tsx        # >= 1
grep -c "vite-plugin-pwa/client" /Users/omar/projects/math-tutor/tsconfig.app.json # >= 1
ls /Users/omar/projects/math-tutor/public/icons/*.png | wc -l           # 4
```
</verification>

<success_criteria>
- pwa-assets.config.ts exists at project root with minimal2023Preset config
- Four PNG icon files exist in public/icons/ (icon-192.png, icon-512.png, icon-512-maskable.png, apple-touch-icon.png), all non-blank
- vite.config.ts exports pwaOptions with manifest (name/short_name/display/theme_color/background_color/start_url/scope/icons per D-08 through D-13) and workbox with audio glob and navigateFallback
- index.html has all five Apple PWA meta/link tags; no manual manifest link
- tsconfig.app.json types array includes "vite-plugin-pwa/client"
- src/main.tsx registers SW with immediate: true before ReactDOM.createRoot
- src/test/pwa.test.ts has 6 passing tests covering PWA-01 and PWA-02 config
- Full Vitest suite passes (0 regressions)
</success_criteria>

<output>
Create `.planning/phases/06-pwa-offline/06-01-SUMMARY.md` when done.
</output>
