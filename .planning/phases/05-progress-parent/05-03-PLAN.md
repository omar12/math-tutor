---
phase: 05-progress-parent
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/components/ProgressBar.tsx
  - src/screens/ParentScreen.tsx
  - src/screens/ParentScreen.test.tsx
  - src/screens/HomeScreen.tsx
  - src/screens/HomeScreen.test.tsx
autonomous: true
requirements:
  - PROG-02
  - PAR-02

must_haves:
  truths:
    - "ParentScreen shows one progress bar row per topic (addition, subtraction, word-problems) — all three always visible"
    - "When no sessions have been recorded, ParentScreen shows a friendly empty-state message instead of 0% bars"
    - "When sessions exist, each progress bar fills proportionally to that topic's accuracy and shows a percentage"
    - "Done button on ParentScreen navigates to / (HomeScreen)"
    - "HomeScreen lock icon navigates to /pin (not /parent)"
    - "HomeScreen auto-selects the lesson from the lowest-accuracy topic when Start Learning is tapped"
    - "If no TopicProgress data exists, lesson selection falls back to curriculum order (first lesson)"
    - "Lesson sort does not mutate the exported lessons array — spread before sort"
  artifacts:
    - path: "src/components/ProgressBar.tsx"
      provides: "Reusable topic progress bar row component"
      exports: ["ProgressBar"]
    - path: "src/screens/ParentScreen.tsx"
      provides: "Parent dashboard with per-topic accuracy bars"
      contains: "useLiveQuery.*topicProgress"
    - path: "src/screens/ParentScreen.test.tsx"
      provides: "Tests for PAR-02 — empty state, accuracy display, Done button"
      exports: []
    - path: "src/screens/HomeScreen.tsx"
      provides: "Lock icon navigating to /pin; adaptive lesson selection"
      contains: "navigate.*pin"
    - path: "src/screens/HomeScreen.test.tsx"
      provides: "Tests for PROG-02 — adaptive ordering, lock icon navigation"
      exports: []
  key_links:
    - from: "src/screens/ParentScreen.tsx"
      to: "src/db/db.ts (topicProgress table)"
      via: "useLiveQuery(() => db.topicProgress.toArray())"
      pattern: "useLiveQuery.*topicProgress"
    - from: "src/screens/HomeScreen.tsx"
      to: "src/db/db.ts (topicProgress table)"
      via: "useLiveQuery(() => db.topicProgress.toArray()) + sortedLessons()"
      pattern: "useLiveQuery.*topicProgress"
    - from: "src/components/ProgressBar.tsx"
      to: "src/screens/ParentScreen.tsx"
      via: "ProgressBar component rendered for each of 3 topics"
      pattern: "ProgressBar"
---

<objective>
Build the parent dashboard and wire HomeScreen adaptive lesson ordering. This plan covers three tight sub-concerns: (1) ProgressBar component that renders a single topic row, (2) ParentScreen replacement with useLiveQuery-driven topic display and Done button, (3) HomeScreen updates for the lock icon (navigating to /pin) and adaptive lesson auto-selection.

Purpose: Delivers PAR-02 (parent sees accuracy per topic) and PROG-02 (weakest-topic lesson surfaced first). These are the user-visible payoffs of the session recording wired in Plan 01.

Output: ProgressBar.tsx, ParentScreen.tsx (replacing stub), ParentScreen.test.tsx, HomeScreen.tsx (lock icon + adaptive ordering), HomeScreen.test.tsx (extended).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-progress-parent/05-CONTEXT.md
@.planning/phases/05-progress-parent/05-RESEARCH.md
@.planning/phases/05-progress-parent/05-01-SUMMARY.md

<interfaces>
From src/db/db.ts:
```typescript
export interface TopicProgress {
  topic: Topic          // primary key: 'addition' | 'subtraction' | 'word-problems'
  accuracy: number      // 0–1 float
  attemptCount: number
  lastPracticed: ISODateString
}
export { db }  // db.topicProgress: EntityTable<TopicProgress, 'topic'>
```

From dexie-react-hooks:
```typescript
import { useLiveQuery } from 'dexie-react-hooks'
// Returns undefined while loading; always guard before use
const progress = useLiveQuery(() => db.topicProgress.toArray())
if (progress === undefined) return <LoadingSpinner />
```

From src/curriculum/index.ts:
```typescript
export const lessons: Lesson[]
// Lesson includes: id: string, topic: Topic, grade: 1|2|3, order: number (curriculum position)
```

From src/curriculum/types.ts:
```typescript
export type Topic = 'addition' | 'subtraction' | 'word-problems'
export interface Lesson {
  id: string
  title: string
  topic: Topic
  grade: 1 | 2 | 3
  order: number
  // ...narration, steps
}
```

Design tokens (src/index.css @theme):
- --color-accent: #3B82F6 (blue — bar fill when accuracy < 0.7)
- --color-success: #22C55E (green — bar fill when accuracy >= 0.7)
- --color-primary: #FF6B35 (orange — lock icon, active states)
- --color-surface: #FFF8F0 (background)
- --color-on-surface: #1C1917 (text)

Current HomeScreen (src/screens/HomeScreen.tsx) — what to change:
- Remove: text button "Parent" navigating to navigate('/parent')
- Add: subtle lock icon button in top-right corner, onClick → navigate('/pin')
- Add: useLiveQuery for topicProgress to compute adaptive lesson selection
- Change: "Start Learning" onClick to navigate to /lesson/${selectedLesson.id} using the weakest-topic lesson
```typescript
// Current (remove this):
<button onClick={() => navigate('/parent')}>Parent</button>
// Replace the parent button AND update Start Learning navigation
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProgressBar component + ParentScreen (replace stub) with tests</name>
  <files>src/components/ProgressBar.tsx, src/screens/ParentScreen.tsx, src/screens/ParentScreen.test.tsx</files>
  <read_first>
    - src/screens/ParentScreen.tsx — read the current stub; this will be fully replaced
    - src/db/db.ts — TopicProgress interface fields (topic, accuracy, attemptCount, lastPracticed)
    - src/screens/PracticeScreen.test.tsx — read render helper, MemoryRouter, beforeEach pattern for db setup
    - src/db/db.test.ts — read db.topicProgress.clear() / db.topicProgress.put() pattern
    - .planning/phases/05-progress-parent/05-RESEARCH.md — ProgressBar component sketch (Code Examples section), D-06 through D-10 decisions
  </read_first>
  <behavior>
    - ParentScreen.test.tsx: when db.topicProgress is empty, renders the empty-state message "No practice sessions yet" (exact substring)
    - ParentScreen.test.tsx: when db.topicProgress is empty, does NOT render any element with role="progressbar"
    - ParentScreen.test.tsx: when topicProgress has one record for 'addition' with accuracy=0.78, renders a progressbar with aria-valuenow=78
    - ParentScreen.test.tsx: all three topic labels always render: 'Addition', 'Subtraction', 'Word Problems' (display names, not raw Topic keys)
    - ParentScreen.test.tsx: Done button has text "Done" and clicking it navigates to '/'
    - ProgressBar component: role="progressbar" aria-valuenow={pct} aria-valuemin={0} aria-valuemax={100} on the fill div
    - ProgressBar component: fill div uses bg-success when accuracy >= 0.7, bg-accent when accuracy < 0.7
    - ProgressBar component: when hasData=false, renders '—' instead of a percentage and no fill div
  </behavior>
  <action>
    Create src/components/ProgressBar.tsx. Export a named function ProgressBar (not default export). Props: { label: string, accuracy: number, hasData: boolean }. When hasData is false, render the label and '—' with no progressbar element. When hasData is true: compute pct = Math.round(accuracy * 100); fillColor = accuracy >= 0.7 ? 'bg-success' : 'bg-accent'; render a label row (flex justify-between: label on left, pct% on right) and a track div (h-4 rounded-full bg-on-surface/10 overflow-hidden) containing the fill div with role="progressbar", aria-valuenow={pct}, aria-valuemin={0}, aria-valuemax={100}, aria-label={`${label}: ${pct}%`}, className={`h-full rounded-full ${fillColor} transition-all`}, style={{ width: `${pct}%` }}.

    Create src/screens/ParentScreen.test.tsx. Use MemoryRouter with initialEntries=['/parent'] and a Route for /parent and a Route for / (to verify Done navigation). Import db from '../db/db'. In beforeEach: db.topicProgress.clear(). Test empty state by rendering with no topicProgress records. Test with data by calling db.topicProgress.put({ topic: 'addition', accuracy: 0.78, attemptCount: 3, lastPracticed: '2026-05-17' as ISODateString }) in beforeEach of that describe block.

    Replace src/screens/ParentScreen.tsx with full implementation. Imports: useLiveQuery from 'dexie-react-hooks'; useNavigate from 'react-router'; db from '../db/db'; ProgressBar from '../components/ProgressBar'; Topic from '../curriculum/types'.

    const progress = useLiveQuery(() => db.topicProgress.toArray()). If progress === undefined, return loading state. const hasAnyData = progress.length > 0.

    Define TOPICS array: [{ key: 'addition' as Topic, label: 'Addition' }, { key: 'subtraction' as Topic, label: 'Subtraction' }, { key: 'word-problems' as Topic, label: 'Word Problems' }].

    For each entry in TOPICS, look up progress.find(p => p.topic === entry.key) to get the TopicProgress (may be undefined). Pass label=entry.label, accuracy=found?.accuracy ?? 0, hasData=Boolean(found) to ProgressBar.

    Empty state: when !hasAnyData, render the friendly message "No practice sessions yet. Come back after your child completes a lesson!" and hide the ProgressBar rows.

    Done button: className="bg-accent text-white text-xl font-bold rounded-3xl px-10 py-4 min-h-[56px]", style={{ touchAction: 'manipulation' }}, onClick={() => navigate('/'). Button text: "Done".

    Screen heading: h1 "Parent Dashboard". Screen container: 100dvh, safe-area insets, bg-surface, flex col items-center, px-6, py-8, gap-6.
  </action>
  <verify>
    <automated>npx vitest run src/screens/ParentScreen.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    All ParentScreen tests pass. Empty state message renders when no sessions. Three topic labels always present. Progressbar has correct aria-valuenow. Done button navigates to /. ProgressBar fills with bg-success at >= 70%, bg-accent below. No fill div rendered when hasData=false.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HomeScreen — lock icon, adaptive lesson selection, extended tests</name>
  <files>src/screens/HomeScreen.tsx, src/screens/HomeScreen.test.tsx</files>
  <read_first>
    - src/screens/HomeScreen.tsx — full file; note the existing "Parent" button and "Start Learning" navigate('/lesson') call
    - src/screens/HomeScreen.test.tsx — full file; read all existing tests before adding new ones (must not break existing: 'renders the Start Learning button', 'renders Remy the Fox')
    - src/curriculum/index.ts — lessons export and its shape (id, topic, order)
    - src/db/db.ts — TopicProgress interface and db.topicProgress reference
    - .planning/phases/05-progress-parent/05-RESEARCH.md — Pattern 5 (adaptive lesson ordering), Pitfall 3 (navigate /parent not /pin), Pitfall 6 (sort mutates array)
    - .planning/phases/05-progress-parent/05-CONTEXT.md — D-01 (lock icon subtle, top-right or bottom-right), D-14 through D-16 (adaptive ordering decisions)
  </read_first>
  <behavior>
    - HomeScreen.test.tsx: existing test "renders the Parent button" must be UPDATED — the text "Parent" no longer exists; test should instead assert a lock icon button is present (use aria-label="Parent access" or similar)
    - HomeScreen.test.tsx: lock icon button's onClick navigates to /pin (not /parent)
    - HomeScreen.test.tsx: when topicProgress has addition accuracy=0.4 and subtraction accuracy=0.8, Start Learning navigates to /lesson/${firstAdditionLesson.id} (weakest topic first)
    - HomeScreen.test.tsx: when topicProgress has equal accuracy for all topics, Start Learning navigates to /lesson/${lessons[0].id} (curriculum order — first lesson)
    - HomeScreen.test.tsx: when no topicProgress data exists, Start Learning navigates to /lesson/${lessons[0].id} (fallback to curriculum order)
    - HomeScreen.test.tsx: Start Learning button still meets min-h-[64px] (existing test passes unchanged)
  </behavior>
  <action>
    In src/screens/HomeScreen.tsx:

    Add imports: useLiveQuery from 'dexie-react-hooks'; lessons from '../curriculum/index'; TopicProgress from '../db/db' (already imports db).

    Add const topicProgress = useLiveQuery(() => db.topicProgress.toArray()). While topicProgress === undefined, render a loading state (same full-screen surface div, just no buttons yet — prevents flash).

    Add a pure function sortedLessons(allLessons: typeof lessons, progress: TopicProgress[] | undefined): typeof lessons — spreads allLessons before sorting ([...allLessons].sort(...)). Sort key: accuracy of lesson.topic from progress map, lowest first; unknown topic defaults to 1.0 (not struggling); tie-break by lesson.order. Returns the sorted array.

    Compute const orderedLessons = sortedLessons(lessons, topicProgress ?? []). const targetLesson = orderedLessons[0].

    Change Start Learning button onClick to: () => navigate(`/lesson/${targetLesson.id}`).

    Remove the "Parent" text button entirely. Add a lock icon button positioned absolute in the top-right corner: className="absolute top-4 right-4 min-h-[44px] min-w-[44px] flex items-center justify-center text-on-surface/30 active:text-on-surface/60", style={{ touchAction: 'manipulation' }}, aria-label="Parent access", onClick={() => navigate('/pin'). Icon: a simple SVG lock (outline, 24×24). The outer screen div needs position: relative (or add className="relative") to contain the absolute lock button.

    In src/screens/HomeScreen.test.tsx:

    Update existing "renders the Parent button" test to use getByRole('button', { name: /parent access/i }) instead of getByText('Parent'). Update the assertion to confirm this button is present.

    Add new describe block for adaptive ordering: seed db.topicProgress in beforeEach with various accuracy values. Assert that Start Learning navigates to the lesson from the weakest topic. Use fireEvent.click on Start Learning, then check that navigate was called with the correct /lesson/${id} path (mock useNavigate or assert the MemoryRouter location changed — use a test Route that captures the navigation target).

    Add test: lock icon onClick navigates to /pin.

    Add db.topicProgress.clear() in a top-level beforeEach to prevent cross-test contamination.
  </action>
  <verify>
    <automated>npx vitest run src/screens/HomeScreen.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    All HomeScreen tests pass (existing + new). Lock icon present with aria-label="Parent access". Lock icon navigates to /pin. Start Learning navigates to /lesson/${weakestTopicLesson.id} when topicProgress data exists. Falls back to curriculum order (lessons[0].id) when no data. sortedLessons never mutates the imported lessons array. Full suite green: npx vitest run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ParentScreen → topicProgress table | Read-only display; no user input crosses this boundary |
| HomeScreen → topicProgress table | Read-only for adaptive sort; user cannot influence accuracy data directly from HomeScreen |
| HomeScreen lock icon → /pin | Navigation only — no data transfer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-06 | Elevation of Privilege | HomeScreen "Parent" button navigating directly to /parent | mitigate | Remove the existing navigate('/parent') button entirely (per D-01); replace with lock icon navigating to /pin; the old direct route is eliminated from UI |
| T-5-07 | Information Disclosure | ParentScreen showing accuracy data without PIN | accept | ParentScreen is only reachable via /parent, which is only navigated to by PinScreen after successful verifyPin(); HomeScreen no longer links directly to /parent; v1 local-only threat model accepts this (no network surface) |
| T-5-08 | Tampering | sortedLessons mutating the lessons array | mitigate | Use [...lessons].sort(...) spread-before-sort pattern; curriculum order immutable for subsequent renders |
| T-5-09 | Denial of Service | useLiveQuery undefined crash in ParentScreen or HomeScreen | mitigate | Explicit undefined guard before any .map() or .find() call; loading state rendered while query resolves |
| T-5-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this phase |
</threat_model>

<verification>
After both tasks complete:

```
npx vitest run --reporter=verbose
```

Full suite must be green. Also verify structural correctness:

```
grep -n "navigate.*pin" src/screens/HomeScreen.tsx
```
Expected: at least one line with navigate('/pin').

```
grep -n "navigate.*parent" src/screens/HomeScreen.tsx
```
Expected: zero results (direct /parent navigation removed).

```
grep -n "role.*progressbar" src/components/ProgressBar.tsx
```
Expected: one result with role="progressbar".
</verification>

<success_criteria>
- ProgressBar.tsx renders label, percentage, filled bar with correct color (bg-success >= 70%, bg-accent below), and hasData=false state with '—'
- ParentScreen shows all three topic rows always; friendly empty-state message when no sessions; Done button navigates to /
- HomeScreen lock icon (aria-label="Parent access") navigates to /pin
- HomeScreen Start Learning navigates to /lesson/${weakestTopicLesson.id} when TopicProgress data exists
- HomeScreen falls back to /lesson/${lessons[0].id} when no TopicProgress data
- Existing HomeScreen tests still pass (Start Learning present, min-h-[64px], Remy visible)
- All ParentScreen.test.tsx and HomeScreen.test.tsx tests pass
- PROG-02 and PAR-02 requirements covered by automated tests
- Full Vitest suite green
</success_criteria>

<output>
Create `.planning/phases/05-progress-parent/05-03-SUMMARY.md` when done.
</output>
